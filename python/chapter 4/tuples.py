# creating a tuple using paranthesis
t = (1,2,3,4)
# printing the elements of tuple
# print(t[0])

# cannot update the value of tuple
# t[0] = 34

# empty tuple
# t1=()                   #empty tuple
t1 = (1,)                #comma always be used when it having one element
print(t1)
t2 = (1,2,3,4,3)         #more then one elements
print(t2)

#tuple methods
a = (7,0,8,0,0,9)

print(a.count(0))        #this used to count the number of elements like 0

print(a.index(9))         # used to know the index number of element



